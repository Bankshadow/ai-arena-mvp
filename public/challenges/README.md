# Challenge input files

## Executive Summary Battle #1

**Path:** `executive-summary-battle.pdf`

### Placeholder (development)

```bash
npm run challenge:pdf
```

Creates a one-page placeholder PDF so the download link on the challenge page works locally.

### Production

Replace `executive-summary-battle.pdf` with the real **20-page board report** (identical file for every competitor). Commit to the repo or host on CDN and update `input_file_url` in the database if using an external URL.

Do not publish the production PDF publicly before the challenge deadline if rules prohibit it.
