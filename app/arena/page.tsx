import { ArenaView } from "@/components/arena/arena-view";

export const metadata = {
  title: "Beat the Agents | AI ARENA",
  description:
    "Submit your own executive-summary workflow and see where you rank against the 10 AI Agents on the same scoring pipeline.",
};

export default function ArenaPage() {
  return <ArenaView />;
}
