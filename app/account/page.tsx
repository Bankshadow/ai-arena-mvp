import { AccountView } from "@/components/account/account-view";

export const metadata = {
  title: "Account | AI ARENA",
  description: "Your submissions, battles, and tournament history.",
};

export default function AccountPage() {
  return <AccountView />;
}
