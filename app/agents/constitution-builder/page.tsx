import { ConstitutionBuilderView } from "@/components/constitution/constitution-builder-view";

export const metadata = {
  title: "Constitution Builder · AI ARENA",
  description: "Design agent operating specifications — role, policies, output contract, evaluation rules.",
};

export default function ConstitutionBuilderPage() {
  return <ConstitutionBuilderView />;
}
