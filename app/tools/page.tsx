import { ToolRegistryView } from "@/components/tool-arena/tool-registry-view";

export const metadata = {
  title: "Tools | AI ARENA",
  description: "Tool plugin registry for Tool Arena.",
};

export default function ToolsPage() {
  return <ToolRegistryView />;
}
