import { StackBuilderView } from "@/components/marketplace/stack-builder-view";

export const metadata = {
  title: "Stack Builder | AI ARENA",
  description: "Compose tournament-tested workflow stacks and export to Cursor or Claude Code.",
};

export default function StackBuilderPage() {
  return <StackBuilderView />;
}
