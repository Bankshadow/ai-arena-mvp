import { VectorsDashboardView } from "@/components/vector/vectors-dashboard-view";

export const metadata = {
  title: "Vectors | AI ARENA",
  description: "Vector Memory Layer — local semantic index dashboard.",
};

export default function VectorsPage() {
  return <VectorsDashboardView />;
}
