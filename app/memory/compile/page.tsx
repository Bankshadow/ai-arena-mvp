import { MemoryCompileView } from "@/components/memory/memory-compile-view";

export const metadata = { title: "Compile | AI ARENA Memory" };

export default function MemoryCompilePage() {
  return <MemoryCompileView />;
}
